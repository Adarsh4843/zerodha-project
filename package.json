{
  "name": "zerodha-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix Frontend && npm install --prefix Dashboard && npm install --prefix Backend",
    "build": "npm run build --prefix Frontend && npm run build --prefix Dashboard",
    "start": "npm run start --prefix Backend"
  }
}
