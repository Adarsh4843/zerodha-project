{
  "name": "zerodha-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Frontend && npm install && npm run build && cd ../Dashboard && npm install && npm run build",
    "start": "cd Backend && npm install && node server.js"
  }
}
